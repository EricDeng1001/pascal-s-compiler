program test(input, output);
var x,y: integer;
int: integer;
bol : boolean;
chr: char;
rl: real;
function fun(a:real; b:char) : integer;  
begin
	
end;
begin
    if bol = z then
        int := 1
    else
        int := 2;
    m := k;
    p := mis;
    read(r, y);
    fun(chr)
end.
