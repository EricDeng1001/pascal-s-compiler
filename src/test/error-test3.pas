program test input out);
var x,y: integer
c: char;
d: real;
z, z1, z2, z3, z4: array[1..9] of integer
function gcd(a b, c: integer):integer;
var f:integer;
begin
    if b = 0 then	gcd := a
    else	gcd := gcd(b, a mod b)
end;
function error(123: integer);
var error: integer;
gcd: real;
begin
    OPOPOPOPO
end;
begin
    read(x, y);
    write(gcd(x, y))
end

