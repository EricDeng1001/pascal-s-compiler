program test input output);
var x,y: integer;
c charar;
d: really;
z, z1, z2, z3, z4: array [1 9] of integer;
q4: array [ 1 . 3 ] of char
function gcd(a b, c: integer) integer;
var f:integer;
begin
    if b = 0 then	gcd := a
    else	gcd := gcd(b, a mod b)
end;
procedure error(a: integer);
var error: integer;
begin
    OPOPOPOPO
end;
begin
    read(x, y);
    write(gcd(x, y))
end

