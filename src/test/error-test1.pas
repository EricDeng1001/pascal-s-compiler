program test(input, output)
x, a, b, c, d, e: integer;
function test(a,b:integer; c,d:real):real;
begin
    test := c + d
end;
begin
    x := 4.3 / 2
    x := 4.3 div 2
    y := x = y;
    x := test(a, b, c, d)
end.
